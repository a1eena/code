#include <stdio.h>  
            int main()
          {
                double  a, b, product =1;
                printf("Enter two number:");
                    scanf("%lf %lf", &a , &b);

                  product = a * b;

               printf("%lf " , product);
    
             return 0;

        }